Options -MultiViews
RewriteEngine On
RewriteBase /pet/

# Nếu là file hoặc thư mục thật thì cho qua
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Tất cả còn lại redirect về index.html
RewriteRule ^ index.html [L]